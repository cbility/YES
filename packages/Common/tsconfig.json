{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "outDir": "./dist", /* Specify an output folder for all emitted files. */
        "rootDir": "./src"
    },
    "include": [
        "src/**/*",
    ],
    "exclude": [
        "node_modules",
    ]
}